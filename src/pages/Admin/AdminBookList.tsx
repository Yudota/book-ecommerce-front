import React from 'react';

interface BookListProps { }

const AdminBookList: React.FC<BookListProps> = (props) => {
  return (
    <>
      Admin Lista livros
    </>
  );
};

export default AdminBookList;
